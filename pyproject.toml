[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "python-efecte"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["requests"]

[tool.setuptools.packages.find]
include = ["efecteClient*"]
